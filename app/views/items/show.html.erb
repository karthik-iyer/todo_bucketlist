<% title "Item" %>

<table>
	<tr><td>
  <strong>Item Name :</strong></td><td>
  <%= @item.name %></td></tr>
</table>
	<table>
	<%for list in @item.lists %>

	<tr><td><%= list.title %> </td><td><%= list.description %></td></tr>

	<%end%>
	<tr><td><strong>Item Record Actions</strong></td><td>&nbsp;&nbsp;&nbsp; <%= link_to "Edit", edit_item_path(@item) %> |&nbsp;&nbsp;&nbsp; <%= link_to "Destroy", @item, :confirm => 'Are you sure?', :method => :delete %> &nbsp;&nbsp;&nbsp;| <%= link_to "View All", items_path %> </td></tr>
		</table>
